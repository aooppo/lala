# Synthetic video fixtures

Automated tests create tiny PNG, WAV, and MP4 files at runtime. They are synthetic and are never
approved Lady LaLa inputs, never copied into production manifests, and never sent to a provider.
